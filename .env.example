# Spotify requires redirect URI to use 127.0.0.1 (not localhost).
# Use the port where your app is served (e.g. 5173 for Vite dev).
# Full redirect URI in Spotify Dashboard: http://127.0.0.1:5173/api/callback
SPOTIFY_REDIRECT_URI=http://127.0.0.1:5173
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
